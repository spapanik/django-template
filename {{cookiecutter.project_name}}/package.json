{
    "name": "{{cookiecutter.project_name}}",
    "version": "0.1.0",
    "private": true,
    "description": "{{cookiecutter.description}}",
    "authors": [
        "{{cookiecutter.author_name}} <{{cookiecutter.author_email}}>"
    ],
    "scripts": {
        "format": "prettier --list-different --write \"src/**/*.{js,scss}\"",
        "lint": "prettier --check \"src/**/*.{js,scss}\" && eslint --max-warnings 0 --ext .js src/"
    },
    "devDependencies": {
        "eslint": "^8.16.0",
        "eslint-config-prettier": "^8.3.0",
        "prettier": "^2.6.2"
    }
}
